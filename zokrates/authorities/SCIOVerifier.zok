import "hashes/sha256/512bitPacked" as sha256packed;

/*
score: achieved score in scio test
threshold: required threshold for CTU
*/
def main(private field score, field threshold) -> bool {
    assert(score >= threshold);
    return true;
}
